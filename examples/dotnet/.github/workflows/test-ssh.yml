# ─────────────────────────────────────────────────────────────
# 🔌 Test SSH — เช็คต่อ Prod เท่านั้น (secrets: PROD_SSH_*)
# ─────────────────────────────────────────────────────────────
name: Test SSH Connection (Prod)

on:
  workflow_dispatch:

jobs:
  test-ssh:
    runs-on: self-hosted
    steps:
      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.PROD_SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p ${{ secrets.PROD_SSH_PORT || 22 }} -H ${{ secrets.PROD_SSH_HOST }} >> ~/.ssh/known_hosts 2>/dev/null

      - name: Test SSH
        run: |
          ssh -i ~/.ssh/deploy_key \
              -o StrictHostKeyChecking=no \
              -p ${{ secrets.PROD_SSH_PORT || 22 }} \
              -l "${{ secrets.PROD_SSH_USERNAME }}" \
              ${{ secrets.PROD_SSH_HOST }} \
              "echo '✅ Prod SSH OK' && whoami && pwd"

      - name: Cleanup SSH key
        if: always()
        run: rm -f ~/.ssh/deploy_key
